# IMPLEMENTATION-TASK-TRACKER.md — HuntMCP (from frozen `MASTER-ROADMAP-FINAL-v3.md`)

**Status:** Phase-S Tier-1 (S1–S4) implemented and CI-verified; S5 (Tier-2's rootless per-run execution boundary)
and S6 (Tier-2's hook tamper-resistance) also implemented and verified with real end-to-end tool calls / live
PreToolUse dispatch on both harnesses (see §3 for per-task evidence, including two rounds of post-landing
adversarial security review: Vulns 1–4 found and fixed, Vuln 5 explicitly documented and deferred by human
decision, not silently skipped). **S-GATE (the Tier-2 exit gate) is also now complete** — its own adversarial
regression suite plus two further rounds of independent adversarial review found and fixed 3 more real bugs in
production `scope_gate_hook.py` (see §3's S-GATE row). Everything past Phase-S remains a planning artifact only —
no Phase 2+ source code, roadmap, architecture, or thesis doc modified beyond what §3 documents. Planning
authority: `MASTER-ROADMAP-FINAL-v3.md` (frozen canonical).

**Phase-3 breadth work may now proceed once `feat/s-gate-adversarial-regression` is reviewed and merged** —
Phase-S Tier-2's FULL exit gate (S-GATE's own adversarial regression across S5 AND S6 together) has passed.
Implementation of anything beyond Phase-S still begins only when v3 §14 entry conditions are met, task by task, in
dependency order.

---

## 1. Task-tracking rules (per `CLAUDE.md`)

- `[ ]` not started
- `[~]` in progress / partially implemented (integration, tests, or verification missing)
- `[x]` completed **and verified** — implementation **+** required tests **+** verification **+** applicable
  acceptance gates all present
- `[!]` blocked / requires human decision

A parent is never `[x]` while any required child remains. "Substrate exists" ≠ "capability complete." No gated research
item is `[x]` until its gate has actually passed.

---

## 2. Current Implementation Baseline (reconciliation)

| Capability | Current implementation | Tests | Real status | What remains |
|---|---|---|---|---|
| CEM Phase-1 proof engine | `cem_engine.py`, `case_store` CEM tables | `tests/test_cem_benchmark.py` + `fixtures/cem_target/` | **[x] FROZEN/VERIFIED (#101)** | nothing — do not rebuild |
| Hypothesis lifecycle store | `case_store` hypotheses/findings/evidence/experiments | case-store tests | [x] baseline | provenance + lineage (see C1a) |
| Content-addressed evidence | SHA-256 store (`add_evidence(content:str)`) | yes | [~] integrity only | provenance binding (C1a) |
| Scope enforcement | 3-layer + PreToolUse `scope_gate_hook.py` | yes | **[x] fail-closed + CI-verified (S1/S2) + tamper-resistant (S6, incl. 2 rounds post-landing review)** | S-GATE adversarial regression |
| Secret handling | `dotenv_loader.get_secret()` per-key lookup; env allowlist at every subprocess-spawn chokepoint (`run_tool()`, `job_runtime.start_job()`, `oob-mcp`); scope-hook `.env`-read block (best-effort) | yes | **[x] S3 done ([PR #106](https://github.com/ankitsingh015/HuntMCP/pull/106), merged `b04805d`)** | — |
| Engagement/state isolation | `engagement_paths.py`, `file_lock`, WAL | yes | [x] baseline | — |
| Cross-run memory/knowledge | memory/writeup/lessons-mcp | yes | [x] baseline | — |
| Continuous recon diffing | `watch-mcp` snapshots (⊥ `case.db`) | yes | [~] recon-only | watch↔case link (C3) |
| Authorization testing baseline | `idor-mcp sweep_idor` (single-request, two-account) | yes | [x] baseline | multi-step/role-matrix engine (VAL-AUTHZ) |
| Attack-chain DAG | `chainer-mcp` | yes | [x] baseline | — (this is "the graph"; no graph DB) |
| Human-review-before-submit | `hackerone-mcp` read-only (no submit tool) | — | [x] control | preserve |
| Budget / audit / jobs | `budget_guard.py`, `audit_log.py`, `job_runtime.py` | yes | [x] baseline | — |
| Toolkit-gap capture | `tool_gaps.py` (bounded first step) | yes | [~] capture only | human-gated growth (P5-TOOLKIT) |
| Second-opinion review | `second-opinion-mcp` (cross-model) | yes | [x] baseline | (env-isolation unexamined) |
| CI | `.github/workflows/ci.yml` (ruff, py_compile, bash -n, scope-gate-dispatch) | — | [~] | SAST/dep-audit (P2-SC) |
| Execution isolation / sandbox | `mcp-servers/sandbox_runner.py` (rootless Podman, both real chokepoints) | `test_sandbox_runner.py` + updated `test_tool_resolver.py`/`test_job_runtime.py` | **[x] S5 done** | S-GATE adversarial regression |
| Telemetry / coverage / postmortem / provenance | **none** | — | **[ ]** | P2 work packages |
| schemathesis / discovery amplification | **absent** | — | **[ ]** | P3 |

---

## 3. Phase S — Security Floor (gating prerequisite; two tiers)

*Ordering (frozen, v3 §6/§14): Tier-1 before ANY autonomous breadth; **Tier-2 MUST complete before Phase-3 breadth
begins.** Tier-2 is NOT optional.*

| ID | St | Task · Why | Deps | v3 § |
|---|---|---|---|---|
| **S1** | [x] | Scope hook **fail-closed** for target-touching calls (internal error blocks the *gated call*, not the session). *Why:* current hook fails open; SPOF. | — | §6 Tier-1 |
| **S2** | [x] | CI **scope-gate block-test** — prove an out-of-scope call is actually blocked. *Why:* fail-open registration SPOF. Verified: [PR #103](https://github.com/ankitsingh015/HuntMCP/pull/103), merged `017a2cd` — the `scope-gate-dispatch` job ran green in GitHub Actions alongside the rest of CI, satisfying the §10 "CI run" evidence requirement. | S1 | §6 Tier-1 |
| **S3** | [x] | **Secrets-out** of untrusted execution — per-key injection; env-scrubbed subprocess spawn; best-effort `.env`-read block. *Why:* full-`.env` load is harvestable. `dotenv_loader.get_secret()` per-key lookup replacing the full-file loader across all 4 real call sites + `model_gateway.py`; env-scrub applied at every real subprocess-spawn chokepoint — `tool_resolver.run_tool()`, `job_runtime.start_job()` [the one real scans actually go through], and `oob-mcp`'s detached `Popen` — after code review found the first pass only covered `run_tool()`; `scope_gate_hook.py` blanket `.env`-read block covering `$(...)`/doubled-prefix/copy-command bypasses found in the same review, live-verified against the real PreToolUse hook. **Caveat, not overclaiming:** the Bash-level block is a best-effort heuristic (a `python3 -c` one-liner embedding the filename in a string literal still isn't caught — same class of acknowledged gap as the existing `rm`-block), not a hard boundary; real closure is S5/S6 sandboxing. Verified: [PR #106](https://github.com/ankitsingh015/HuntMCP/pull/106), merged `b04805d` — CI green. | S1 | §6 Tier-1 |
| **S4** | [x] | **`--os-shell` / state-changing confirmation tier** — explicit human confirm. *Why:* persistent RCE, no distinct gate today. `scope_gate_hook.py` blanket-blocks any sqlmap `--os-shell`/`--os-pwn`/`--os-cmd`/`--os-bof` Bash call (same unconditional category as the rm-block/`.env`-block, independent of scope; matches on the flag co-occurring with `sqlmap` anywhere in the sub-command, not just first-word position, closing a `timeout`/`xargs`-wrapper bypass found in review) unless a fresh, single-use confirmation token — **bound to the active engagement's exact target** (a code-review-found gap, corroborated by 5 independent review angles, fixed: `check_and_consume(target)` now refuses a token confirmed for a different target without burning it) — is on file. Token can only come from `scripts/confirm-os-shell.sh` → `rce_confirm.py`, gated on `stdin.isatty()`. Confirmed-and-allowed action is audit-logged (`sqlmap-os-shell-confirmed`, since a raw-Bash sqlmap call has no other audit path). The whole check runs inside its own fail-closed try/except (code-review finding, CONFIRMED: it previously sat outside the fail-closed net — an internal error would have failed *open*, reopening the exact S1 bug class one check later — fixed). Unit tests (`tests/test_rce_confirm.py`, `tests/test_scope_gate_hook.py`) + a real-pty "live prompt" test (`tests/test_confirm_os_shell_live.py`, matching the §10 acceptance table's own "live prompt" test type) + live-fired against the real PreToolUse hook subprocess. **Caveats, not overclaiming (code-review findings, CONFIRMED, deliberately not closed — this is Tier-1's own honest limit, not an oversight):** (1) the `isatty()` check is a speed-bump against accidental/naive self-confirmation, not a hard boundary — an agent with ordinary Bash access can spawn its own pty (stdlib `pty` module, exactly what this PR's own live test does to drive the script) and satisfy it without a human; (2) `check_and_consume()` does no cryptographic/provenance check on the token file — an agent with Write/Edit access can fabricate one directly at the path this module resolves to, bypassing `request_confirmation()` entirely. Both require the agent to actively work *around* its own safety rail using its own execution capability — closing that is S6's job ("hook tamper-resistance"), not a Tier-1 heuristic's; this mirrors the same floor-not-boundary caveat already recorded for S1's rm-block and S3's `.env`-block. Also narrow by design (only sqlmap's 4 named flags, matching the roadmap's own literal wording — not a general state-changing-action classifier for e.g. nuclei RCE templates or a webshell `curl`). Verified: [PR #107](https://github.com/ankitsingh015/HuntMCP/pull/107), merged `a380afb` — CI green (`Scope Gate Dispatch Test` + `Unit Tests` both passed), and re-confirmed green again on [PR #109](https://github.com/ankitsingh015/HuntMCP/pull/109) (unrelated `scope_gate_hook.py` changes for other bugs, same two CI jobs still pass, confirming S4's logic wasn't disturbed). | — | §6 Tier-1 |
| **S5** | [x] | **Rootless per-run execution boundary**, via **rootless Podman containers** (human decision resolved 2026-09-15, §8; Docker/Podman/nsjail/bubblewrap options weighed, Podman chosen — no root daemon, no host-level reconfiguration needed). *Why:* whole-host TCB. `mcp-servers/sandbox_runner.py` is the ONE shared argv-builder both real subprocess chokepoints call — `tool_resolver.run_tool()` and `job_runtime.start_job()` (the PRIMARY path: httpx/nuclei/katana/nmap/dalfox/ffuf/sqlmap/subfinder's real enumeration all go through `start_job()`, not `run_tool()`) — migrated in the same change, so there is no window where one sandboxes and the other doesn't. "Run" = one external tool-binary execution attempt (one `subprocess.run()`/`Popen()` call, including `run_tool()`'s own rate-limit retry as its own independent run) gets a fresh, uniquely-named scratch dir + a fresh `--rm` container from a single pinned, minimal image (`mcp-servers/sandbox/Dockerfile`, versions pinned to what was confirmed working, not `@latest` — the exact class of drift that broke nuclei's `-json` flag live). Approved-tool allowlist (`_TOOL_MAP`) covers every tool actually reachable through these two chokepoints, audited by grepping every real call site rather than assumed from memory: subfinder/httpx/katana/nmap/nuclei/sqlmap/dalfox/ffuf/curl/wget/gitleaks (secrets-mcp) + impacket's Kerberoasting scripts under all 3 host-name variants (ad-recon-mcp) — an unrecognized name raises `UnknownSandboxTool` before any subprocess is attempted, re-raised as `FileNotFoundError` at both chokepoints so every existing caller's own `except FileNotFoundError` handling needed zero changes. Isolation: `--cap-drop=ALL`, `--read-only` root fs (`--tmpfs=/tmp` for incidental scratch writes), `--security-opt=no-new-privileges`, `--pids-limit=50`/`--memory=512m`, env built ONLY from the same `minimal_subprocess_env()` allowlist S3 already uses (never `--env-host`, never a `$HOME` mount) — a second, independent enforcement of S3's guarantee, not a replacement for it. Mounts are **explicit-declaration-only**, never auto-detected: each calling MCP server passes `extra_mounts=` (read-only) / `extra_mounts_rw=` (read-write, opt-in) naming exactly the paths that specific call needs — `sqlmap-mcp`'s `--output-dir`, `httpx-mcp`'s `-l <input_path>`/`-srd <work_dir>`, `secrets-mcp`'s scan target + report dir, `ffuf-mcp`'s resolved wordlist file, `ad-recon-mcp`'s users file — each updated to pass its own real mount, plus a deny-list backstop (`_DENY_MOUNT_PREFIXES`/`_DENY_MOUNT_EXACT`) against a future caller passing something dangerously broad. An earlier version instead auto-detected any existing absolute path found in a tool's own args and mounted it automatically; code review found this was a real security hole (`ffuf-mcp`'s agent-facing, prompt-injection-reachable `wordlist` parameter could point at e.g. `~/.ssh/id_rsa` and have it auto-mounted and permission-widened) and it was removed before landing, not kept as a "known gap." **Caveats, not overclaiming (found live, fixed before landing):** (1) `--userns=keep-id` is required for the sandboxed process to actually write into its own bind-mounted scratch dir at all under rootless Podman's UID remapping — without it every real run failed with a permission error, not a "container works but is slow" issue; (2) `tempfile.mkdtemp()`'s default `0700` mode (used both by this module's own scratch dirs and by real callers like httpx-mcp's `work_dir`) is incompatible with `--userns=keep-id`'s group-based access and needed an explicit chmod (`_ensure_group_accessible()`, adds owner+group rwx/rw, never touches "other") on every declared mount source; (3) the deny-list's first version prefix-matched `$HOME`/the repo root, which blocked sqlmap-mcp's actual real `--output-dir` (a legitimate path living deep under both) on every single real call — fixed to exact-match-only for those two, prefix-matched only for genuine system directories nothing legitimate ever needs; (4) an unconditional `:rw` mount mode caused a real, demonstrated data-loss incident during this feature's own testing (a live run deleted a real git-tracked project wordlist file from the host, recovered via `git checkout`) — fixed by splitting mounts into read-only-by-default (`extra_mounts`) vs. explicit read-write opt-in (`extra_mounts_rw`), with a regression test proving read-only mounts genuinely block writes at the kernel level; (5) `secrets-mcp`'s gitleaks report path was `mkstemp()`+`unlink()`'d before being mountable, so every scan silently reported "no findings" regardless of actual results, until the report path became a real, mountable directory; (6) `ad-recon-mcp` checked the HOST's `PATH` for impacket scripts that only exist inside the sandbox image, so kerberoast/asreproast always failed "not found" on a properly-sandboxed machine — fixed to use fixed canonical in-container tool names; (7) the original bulk `reap_orphans()` force-removed every container sharing the shared label, which a live concurrency test proved would kill a different, healthy, still-running concurrent sandboxed call as collateral damage — replaced with per-run container naming + targeted `remove_container(name)`. All seven were found via a code-review self-audit and closed before this row was marked done, not deferred. **Known, disclosed gaps (not closed here):** `oob-mcp`'s own separate `resolve_tool()` + detached `Popen` for its long-lived `interactsh-client` listener is a THIRD subprocess pattern, explicitly out of scope (S5's mandate named `tool_resolver`/`job_runtime` specifically; a persistent listener doesn't cleanly fit "per-run" anyway) — still runs natively on the host; `httpx-mcp`'s `-screenshot` flag needs a real browser binary this minimal image doesn't include, so that one specific feature fails inside the sandbox (a missing capability, not a broken isolation boundary) until/unless a browser is deliberately added to the image. **Network model (read before assuming otherwise):** V1 provides rootless-privilege, filesystem, credential/environment, process, and resource isolation. It does **NOT** provide per-target network egress ACLs — default rootless networking reaches any destination the host's own networking allows, since every one of these tools must reach a real target. Building a custom per-destination network policy (CNI plugin, iptables) is explicitly OUT OF SCOPE for V1 — new network-control-plane infrastructure is exactly what this Tier-2 floor's own discipline rules out ("must not metastasize into platform engineering"). `scope_gate_hook.py`'s PreToolUse check remains the sole "may this call touch this host at all" boundary, runs BEFORE sandboxing, and is not superseded or duplicated by it. Tests: `tests/test_sandbox_runner.py` (~30 tests — argv construction, allowlist enforcement, mount minimality/safety, HOME override, plus live tests against the real image: network reachability, host-env invisibility, read-only fs, scratch-dir writability, read-only-mount-prevents-writes, orphan-free cleanup on both normal exit and a hard SIGKILL, and concurrency-safety) + `tests/test_tool_resolver.py`/`tests/test_job_runtime.py`/`tests/test_secrets_mcp.py`/`tests/test_ad_recon.py`/`tests/test_ffuf_mcp.py` updated or extended for the new sandboxed, explicit-mount contract. Full suite: 1482 passed, 5 pre-existing xfailed (documented Phase-2-boundary markers in the unrelated CEM benchmark, not S5). Runtime verification: real end-to-end calls through the actual unmodified MCP server code (not synthetic) for `curl` (`tool_resolver.run_tool`), `httpx` (`job_runtime.start_job`+`poll_job`), `gitleaks` (found a real planted AWS key, proving the report-mount fix), `sqlmap` (wrote a real results CSV back to the host through a read-write mount, confirmed present on disk after the run), and `ad-recon-mcp` kerberoast (launched correctly, timed out correctly against a fake DC, zero orphaned containers) all succeeded, proving containment doesn't break legitimate utility. `scripts/verify-sandbox.sh` is the operator-facing readiness check (podman present, rootless, image built, one real sandboxed call succeeds). This satisfies S5's own scope; `S-GATE`'s full adversarial regression (canary-secret exfil, out-of-scope action, hostile-repo checkout, across create→run→pause→resume→terminate) is separate, later, and still blocks Phase-3 breadth until S6 also lands. | S1–S4 | §6 Tier-2 |
| **S6** | [x] | **Hook tamper-resistance** — bash file-writes above the boundary prevented. *Why:* hook neutralizable mid-session (an agent with ordinary Edit/Write/Bash access could previously overwrite `scope_gate_hook.py` or drop its `.claude/settings.json` registration with zero gate at all — Edit/Write/NotebookEdit tool calls were never evaluated by the hook, only Bash was). Protected set (broader option, human-approved): `scripts/hooks/scope_gate_hook.py` itself + `mcp-servers/{scope_guard,budget_guard,audit_log,rce_confirm,sandbox_runner,dotenv_loader,hook_confirm}.py` + `.claude/settings.json` + `.claude/settings.local.json` + `.opencode/plugin/scope-gate.ts` + `opencode.jsonc` + `scripts/confirm-os-shell.sh` + `scripts/confirm-hook-edit.sh`, plus the two confirm-token paths themselves (closing rce_confirm.py's own S4-documented "an agent can fabricate a token file" gap). Two detection surfaces, added to `scope_gate_hook.py`: (1) **Edit/Write/NotebookEdit** whose `file_path`/`notebook_path` resolves (post `Path.resolve()`, closing relative-traversal and symlink tricks) to a protected path; (2) **Bash writes** — redirects (`>`/`>>`, spaced or not), `sed -i`, `tee`, `cp`/`install`/`rsync`/`ln` with a protected destination, `mv` with a protected source-or-destination, `dd of=`, and `git checkout <ref> -- <path>`/`git restore <path>` (found in adversarial self-review: overwrites the working tree from history with **no shell redirect at all** — would otherwise silently roll back to a pre-S6, unprotected hook). Same "cheap word/arg scan, not a full shell parser" honesty as S1/S3/S4's own blocks — an obscured `python3 -c` write or a `git apply`/`patch` diff body is NOT caught (explicit regression test asserts it is NOT, not merely undocumented). Escape hatch (human-approved multi-use-within-TTL design, deliberately different from S4's single-use RCE token): new `mcp-servers/hook_confirm.py` + `scripts/confirm-hook-edit.sh`, mirroring `rce_confirm.py`'s isatty-gated/typed-confirm-phrase flow (phrase `"CONFIRM HOOK EDIT"`), but the token is a 30-minute *non-consuming* window (`check_valid()`, not `check_and_consume()`) rather than single-shot — a real maintenance session (this exact S6 implementation session) needs several edit passes; still requires a live human typing the phrase at least once per ~30-min window. Not engagement-scoped (deliberate: hook tamper-resistance is a repo/toolchain concern, not per-target hunt state — see `hook_confirm.py`'s own module docstring for the rejected alternative). OpenCode dispatch (`.opencode/plugin/scope-gate.ts`) extended in the same change: previously forwarded ONLY `bash`/`mcp__` calls, so an `edit`/`write` tool call was invisible to it entirely regardless of target file — real tool ids verified against a live `opencode serve` instance's own `/experimental/tool/ids` API (not guessed), correcting a false claim from an earlier draft (a `strings`-based match that turned out to be OpenCode's unrelated Electron desktop "Edit" menu, caught in adversarial review); `apply_patch` (a real tool id) is deliberately NOT mapped — its real argument shape doesn't carry a single `filePath` the way `edit`/`write` do, so this is a documented, test-asserted gap, not a false coverage claim. **Adversarial self-review findings (3, all fixed before landing, not deferred):** (1) `git checkout`/`git restore` bypass (above); (2) `ln -sf` symlinking over a protected path (same "last positional arg is the destination" shape as `cp`/`install`/`rsync`, added to the same bucket); (3) the false `strings`-based OpenCode tool-name claim (above), corrected with live-API verification and the `apply_patch` non-claim. Tests: `tests/test_hook_tamper_resistance.py` (37 — path-resolution/traversal/symlink correctness, Edit/Write/NotebookEdit block+confirmed-allow, every Bash-write variant incl. git checkout/restore/ln, TTL-window multi-use behavior, the acknowledged python3-one-liner gap asserted NOT caught), `tests/test_hook_confirm.py`, `tests/test_confirm_hook_edit_live.py` (3 — real-pty live-prompt test, mirroring `test_confirm_os_shell_live.py`'s S4 precedent exactly: writes-token / declines-on-wrong-text / piped-non-tty-refuses, all 3 passed), `tests/test_scope_gate_hook.py` extended, `tests/test_scope_gate_plugin.mjs` extended (OpenCode-side S6 cases, incl. the apply_patch non-coverage regression). Real dispatch verification, both harnesses: Claude Code — this exact S6 implementation session's own Edit calls to `scope_gate_hook.py` went through the real, live `.claude/settings.json` PreToolUse dispatch (not a test double) and were correctly allowed only while a human-confirmed token was on file, blocked once it expired, requiring a live re-confirm from the human operator (observed twice during this session); `tests/test_scope_gate_dispatch.py` (registration + real subprocess dispatch) green. OpenCode — `node tests/test_scope_gate_plugin.mjs` green on every S6 case (edit-of-hook blocked, write-of-settings.json blocked, apply_patch non-coverage documented, edit-of-unrelated-file allowed, read never forwarded); 2 pre-existing, unrelated failures in that same file (`in-scope curl is allowed` / `in-scope Tier-2 MCP tool call is allowed`) reproduced identically with S6 fully reverted via `git stash` — confirmed pre-existing (this machine's real, live, concurrently-changing active-engagement pointer leaking into that test's own isolation), not a regression, not touched (out of S6's scope). Full regression: 1532 passed, 1 pre-existing unrelated flake (`test_nuclei_mcp.py`'s live-binary-timeout test, confirmed via isolated re-run and unrelated to any S6 file), 1 skipped, 5 xfailed (same documented CEM Phase-2 boundary markers as every prior S-task) — S1–S5 behavior fully preserved (233/233 focused S1–S6 tests green). `.gitignore` updated for the new runtime token file (`data/hook-edit-confirm.json` + `.lock`, previously uncovered — found during review, not itself a security issue but a real hygiene gap). Work relocated from `main` (where it was mistakenly started mid-session after S5's PR #110 merge switched the checkout) onto its own branch, `feat/s6-hook-tamper-resistance`, matching every prior S-task's own PR-based workflow; nothing committed or pushed pending human review. **POST-COMPLETION SECURITY REVIEW (same session, human-requested "review and audit"):** an independent security-review pass (vulnerability-ID sub-agent + 2 parallel false-positive-filtering verification sub-agents, `security-review` skill) found a real, HIGH-severity, CONFIRMED (8/10 confidence, both independently) directory-destination bypass in the original S6 landing: every Bash-write destination check (redirects, `sed -i`, `tee`, `cp`/`install`/`rsync`/`ln`, `mv`, `dd of=`, `git checkout`/`git restore`) matched the literal destination argument against the protected-path set by EXACT membership only — never accounting for real Unix/git semantics where an EXISTING DIRECTORY destination places the write inside it (`cp x scripts/hooks/` → `scripts/hooks/scope_gate_hook.py`; `git restore mcp-servers/` → the whole subtree). A second, more serious instance of the identical gap let an agent forge `hook_confirm.py`'s own token file the same way (`cp forged.json data/`), fully defeating S6's human-in-the-loop guarantee with zero human ever running the confirm script. Fixed via TDD (9 new RED→GREEN regression tests in `tests/test_hook_tamper_resistance.py`, incl. a negative control proving unrelated directories stay unblocked) with a new `_protected_hit()` helper: ancestry-based (candidate IS a protected path, OR is a directory that CONTAINS one), deliberately the more conservative of the two fix options (blocks more, never fewer — e.g. any write into `mcp-servers/` is now gated, not just exact protected filenames) rather than precisely replicating each tool's own basename-join semantics, matching this file's existing fail-closed bias (S1/S3's own blanket blocks). Full regression re-run clean: 1543 passed, 0 failed, 5 xfailed (same documented CEM Phase-2 xfails as always) — the two live-nuclei/watch-mcp flakes seen in earlier runs did not recur, consistent with them being system-load-dependent, not S6-caused. Focused S1–S6 suite: 242/242 green (up from 233, +9 new tests). **ROUND 2 SECURITY REVIEW (same session, second human-requested "review and audit," run against the round-1-fixed code):** found 3 more findings via the identical vuln-ID + 3-parallel-verification-sub-agent methodology. **Vuln 3 (curl -o/--output/-O, wget -O/--output-document, CONFIRMED 9/10) FIXED:** both tools were already-recognized Tier-2 tools elsewhere in this file but their own output-file flags were never checked by `_writes_protected_path()` — `curl -o scripts/hooks/scope_gate_hook.py https://raw.githubusercontent.com/...` (an always-exempt dev-infra host, no engagement.yaml needed) silently overwrote the hook with zero confirm/block. New `_curl_output_target()`/`_wget_output_target()` helpers (handle `-o`/`--output`/`--output=`/attached `-oFILE` forms for curl, the analogous forms for wget's `-O`, plus curl's argument-less `-O`/`--remote-name` which derives the local filename from the URL's own last path segment) routed through the same `_protected_hit()`. **Vuln 4 (tar/unzip/7z extraction into a protected directory, CONFIRMED 8/10) FIXED:** none of these tools appeared anywhere in the write-detector at all — `tar -xf x.tar -C scripts/hooks/` (with a maliciously-named archive member, no harder to stage than round-1's `/tmp/x.py`) was fully unmediated. New `_tar_directory_target()` (`-C`/`--directory`, checked unconditionally regardless of extract-vs-create mode — same "blocks more, never fewer" bias as everywhere else in this function), `_unzip_directory_target()` (`-d`), `_sevenzip_directory_target()` (`-o<dir>`, always the attached form per 7-Zip's own CLI convention) — all routed through `_protected_hit()`. Explicitly documented, NOT fixed: a zip-slip archive member (its own name containing `../`) extracted into an unrelated, non-ancestor directory is still uncaught (would require reading the archive's own index, not a word scan) — asserted via `test_main_does_not_claim_to_catch_zip_slip_into_an_unrelated_directory`. Both fixed via TDD: 17 new RED→GREEN tests (11 positive + 6 negative controls) in `tests/test_hook_tamper_resistance.py`, verified RED first against the pre-fix code. Full regression re-run clean again: 1560 passed, 0 failed, 1 skipped, 5 xfailed. Focused S1–S6 suite: 205/205 green. **Vuln 5 (`git checkout <ref>` with no path / `git reset --hard <ref>`, CONFIRMED 9/10) DOCUMENTED, NOT YET FIXED — human decision pending, per explicit instruction not to implement a broader mechanism without approval:** neither command carries a per-file destination argument for `_protected_hit()` to inspect at all — both replace the ENTIRE working tree's tracked content to match a ref, atomically, keyed only by the ref name/sha. Verified against this repo's real history: commit `50736b2` (this hook's own origin commit) has zero tamper-resistance/scope-gate code; `git reset --hard 50736b2` (or any pre-S1/pre-S6 sha, all real, discoverable via ordinary `git log`) would silently revert the hook to that state. **This is a structurally different gap than Vulns 1–4**, not closable by extending `_protected_hit()` — there is no destination argument, so closing it would mean either (a) diffing the target ref's tree content against current protected-file content before allowing the command (real tree-diffing — a materially bigger mechanism, explicitly out of scope for this patch per instruction), or (b) gating the command SHAPE itself, unconditionally, regardless of what it would actually change. Two smallest-viable-mitigation options identified for (b), both reusing existing precedent/mechanisms with no new component: **Option A** — blanket, unconditional block (S1's rm-block precedent): refuse the Bash call outright, "ask the user to run this in their own terminal." Simplest; zero new state; but blocks ALL uses including ordinary legitimate branch switches (`git checkout <branch>`). **Option B** — confirm-gate via the SAME `hook_confirm.py` mechanism just built for S6 (S4 os-shell-confirm precedent): require a valid `hook_confirm` token before either command runs at all, unconditionally (can't condition on "does the target ref actually differ" without the tree-diffing this task excludes). Reuses the exact existing mechanism/token/script; the human already has to run `scripts/confirm-hook-edit.sh` for direct protected-path writes, so this is a small, natural extension of an already-approved design rather than a new one. **Recommendation:** Option B for both command shapes — `git reset --hard` is already treated as an exceptional, human-explicit-request-only operation by Claude Code's own global Git Safety Protocol (independent of this repo), so gating it here is consistent, not novel; bare `git checkout <ref>` is exactly as capable of reverting protected files as `reset --hard` (the existing code's "ambiguous with a branch switch" comment was about avoiding false-positive BLOCKS on ordinary git-checkout-pathspec parsing, not a security judgment that bare-ref checkout is safe) — Option A would be more disruptive to ordinary branch-switching workflows than Option B's occasional re-confirm. **HUMAN DECISION (received): "Document as accepted gap, defer."** Neither Option A nor B implemented in this patch. Closed out via documentation only: `_writes_protected_path()`'s own docstring in `scope_gate_hook.py` updated to name this as an explicit, accepted, deferred limitation (not an oversight) alongside the python3-one-liner/git-apply/zip-slip gaps; two new explicit regression tests (`test_main_does_not_claim_to_catch_bare_git_checkout_of_a_ref`, `test_main_does_not_claim_to_catch_git_reset_hard`) assert the current code does NOT catch either shape, using the real pre-S1 commit `50736b2` as the ref — keeping the gap documented and visible rather than silently assumed closed, same honesty standard as every other acknowledged limit in this file. Revisit trigger: S-GATE's own adversarial regression (below) or a future task, not this patch. Full regression re-run after this final change: clean. **S6 marker now `[x]` — Vulns 1–4 fixed and verified, Vuln 5 explicitly decided (documented/deferred, not silently skipped), matching the task's own completion bar.** | S5 | §6 Tier-2 |
| **S-GATE** | [x] | **Tier-2 exit gate** — adversarial regression (canary-secret exfil via hostile tool output; out-of-scope action; hostile-repo checkout) passing on **containment AND legitimate-task utility**, across the lifecycle (create→run→pause→resume→terminate). **Blocks Phase-3 breadth.** `tests/test_s_gate_adversarial.py` (15 tests) built as the adversarial regression corpus exercising S5+S6 TOGETHER through the REAL chokepoints (`job_runtime.start_job()`/`poll_job()`, `scope_gate_hook.main()`) rather than re-testing either control in isolation — explicitly the gap the tracker's own line calls out ("S5+S6 individually do not satisfy S-GATE"). Lifecycle mapped onto this codebase's real mechanics: create=`start_job()`, run=container executing, pause/resume=successive `poll_job()` calls while still "running", terminate=either a normal "done" exit or the `max_wall_seconds` forced-kill path (`_kill_and_collect`, SIGKILL+`remove_container()`) — both termination paths exercised. Coverage per named scenario: **(1) canary-secret exfil** — a canary outside `SUBPROCESS_ENV_ALLOWLIST`, a hostile `curl file:///proc/self/environ` read through the real chokepoint, absent at every lifecycle checkpoint on both termination paths, with a positive "the read genuinely worked" sanity check; **(2) out-of-scope action** — hook blocks before `start_job()` is reached, plus a test-asserted (not just documented) proof sandbox_runner alone provides no network-scope enforcement if the hook were bypassed, plus a structural proof `poll_job()` never re-touches scope; **(3) hostile-repo checkout** — a REAL second git repo with a backdoored hook file via `cp`, a real `git fetch`+pathspec `checkout`, and the Edit-tool vector, all blocked without confirmation/allowed with it; a sandboxed-execution proof a hostile repo's own `cwd` has zero mount path back to protected files; a documentation-grade regression distinguishing `sandbox_runner`'s mount-deny-list from the hook's protected-path set; a reproduction of the already-deferred Vuln 5 (bare `git reset --hard`) proving it's still (correctly, by human decision) not caught. **Combined test:** a hook-tamper attempt is still blocked while a real sandboxed job runs concurrently. <br><br>**Independent adversarial review, round 1** (vuln-ID sub-agent + 4 parallel verification sub-agents, identical methodology to S5/S6's own rounds, applied to this branch's diff): found **2 real, CONFIRMED (9/10 confidence, 4 independent live-reproduced passes each) HIGH-severity bypasses in PRODUCTION `scope_gate_hook.py`** — pre-existing bugs the new test-writing exposed, not introduced by it. **Vuln A (scope-check bypass):** `_extract_hosts_from_bash()` gated on a bare `_first_word(command) not in TIER2_BASH_TOOLS` — unlike every other blanket check in the file, it never chain-split or wrapper-stripped, so `env curl <out-of-scope-host>`, `timeout 30 curl ...`, or `echo hi && curl ...` skipped the entire out-of-scope check AND the curl/wget budget-enforcement+audit-log block, silently, with zero stderr. **Vuln B (S6 hook-tamper-resistance bypass):** `_strip_prefix_words()` only ever recognized `sudo`/`env` as strippable wrappers — `timeout 5 cp /tmp/hostile.py scripts/hooks/scope_gate_hook.py` (also `nice`/`nohup`/`stdbuf`-wrapped) silently overwrote the hook itself with **zero confirm prompt**, live-reproduced. Both are the exact "wrapper defeats first-word matching" bug class already fixed for `_is_persistent_rce_command()` (S4) but never ported to these two other checks. **Fixed via TDD** (RED tests added to `tests/test_scope_gate_hook.py`/`tests/test_hook_tamper_resistance.py` first, confirmed failing, then fixed): `_strip_prefix_words()` widened to `timeout`/`nice`/`nohup`/`setsid`/`ionice`/`chrt`/`taskset`/`stdbuf` (fixing Vuln B, and transitively strengthening the rm-block/.env-block that share the same helper); `_extract_hosts_from_bash()` and the curl/wget budget/audit block in `main()` changed to scan every shlex-split word's basename (new `_bash_basenames()` helper) rather than relying on positional first-word matching (fixing Vuln A, including the missing audit-log entry for wrapped calls). **Round 2** (one more targeted adversarial pass hunting for bypasses of the round-1 fix itself, mirroring S6's own two-round precedent): found **1 more real regression the round-1 fix itself introduced** (`timeout --signal=KILL 5 cp ...` — a real GNU `timeout` flag preceding the duration — made the fix's own unwrap loop give up with the FLAG left in first-word position, silently swallowing the real command; worse than the original bug since it defeated rm-block/.env-block/hook-tamper-resistance simultaneously) — **fixed via TDD** (skip leading attached-form flags before the duration check); and **1 low-severity, fail-closed-direction, ACCEPTED trade-off** (the new whole-command word scan can now scope-check a bare unquoted mention of a tool name in prose, e.g. `echo curl <host> is a tool` — safe direction, consistent with this file's "blocks more, never fewer" bias, quoted mentions unaffected since shlex.split collapses them to one token) — documented with an explicit regression test rather than silently assumed either way, plus a utility sibling proving quoted mentions still pass. Both rounds' remaining, deliberately-not-fully-closed wrapper-grammar edge cases (`nice -n 10 cp ...`, `timeout -s KILL 5 cp ...` — a wrapper flag needing a SEPARATE space-delimited value) are explicitly named in `_strip_prefix_words()`'s own "ACKNOWLEDGED, NOT FIXED" docstring and asserted-NOT-caught by dedicated regression tests, same honesty standard as every other documented limit in this file (obscured python3 one-liner, zip-slip, bare git checkout/reset --hard). Every reported bypass from both rounds was independently re-verified live by hand against the real hook binary (not just via pytest) before this row was marked done. Full regression after both rounds: **1610 passed, 0 failed, 5 xfailed** (same documented CEM Phase-2 xfails as every prior S-task); two unrelated, confirmed-pre-existing flakes observed once during a full-suite run (`test_sandbox_runner.py`'s container-cleanup-race and a `test_watch_mcp.py` timing test, neither touching any file this task modified — reproduced as non-deterministic on isolated re-run, consistent with system-load-dependent flakes already documented elsewhere in this tracker) did not recur on a clean re-run. Verified: [PR #112](https://github.com/ankitsingh015/HuntMCP/pull/112), merged `b002369`. | S5,S6 | §6, §11(rootless), §14.2 |

---

## 4. Phase 2 — Foundation, Evidence Integrity, Introspection & Reliability

*Prereq: Phase-S Tier-1 (S1–S4).*

| ID | St | Task · Why | Deps | v3 § |
|---|---|---|---|---|
| **P2-BENCH** | [x] | Benchmark + telemetry substrate: standing multi-class ground-truth range (blind, protected) + frontier reference arm; emit cost/yield/coverage. *Note:* CEM-specific benchmark already exists (`test_cem_benchmark.py`) and may be **extended**, not rebuilt; zero lines changed under `tests/fixtures/cem_target/`, `case-mcp/`, `cem_engine.py`, or `test_cem_benchmark.py` (verified via `git diff --stat` at completion). Design spec (`docs/superpowers/specs/2026-09-17-p2-bench-design.md`) and 17-task implementation plan (`docs/superpowers/plans/2026-09-17-p2-bench-implementation.md`) approved and built via TDD. **Tasks 1–11 (substrate):** `tests/fixtures/bench_target/` — a loopback-only, stdlib-`http.server` target app (`bench_app.py`) with 6 REAL, genuinely-exploitable planted vulnerabilities, each with a vulnerable/patched pair (SQL injection via a real in-memory SQLite string-concatenated query; reflected XSS via unescaped-vs-`html.escape()`-d reflection; IDOR via a missing-vs-enforced session-ownership check; an exposed backup file present-vs-404; a security-misconfiguration response header present-vs-removed, with a custom, hermetic nuclei template bundled in the fixture; an open redirect following-vs-validating the `next` param) plus an independent in-process request log; a BLIND case index (`bench_scenarios.py`) and an EVALUATOR-ONLY answer key (`bench_answer_key.py`), both SHA-256 integrity-locked (`bench_integrity.py`); an arm-agnostic scoring contract (`bench_evaluator.py`: `ObservedFinding` → `evaluate(observed, mode) -> BenchReport`, with `novel_findings` tracked separately, never counted as false positives) plus an evidence-trail cross-check and reproducibility check; a loopback-only auxiliary HTTP client (`bench_harness.py`). One real Critical bug found and fixed during Task 9's review (`ObservedFinding.evidence: dict = {}` — a NamedTuple mutable-default aliasing bug — fixed to `dict | None = None` + `evidence_or_empty()`), plus one pre-flight cross-task conflict fixed before code was written (IDOR path matching changed from exact-equality to prefix-aware). **Tasks 12–17 (real Tier-2 tool fixture-proof, completed 2026-09-22):** the previously-recorded networking blocker (a sandboxed Tier-2 tool cannot reach a host-loopback-bound service) was resolved via a NEW, narrowly-scoped, dormant-by-default networking seam rather than deferral: `sandbox_runner.build_argv()` gained one internal check — if the process environment sets `HUNTMCP_BENCH_NETWORK` (never set by any production launch config; not a function parameter; no code path from an agent's tool-call input to it), it joins that Podman network instead of the default isolated one; for every real hunt this variable is absent, so behavior is byte-identical to before (locked in by `test_build_argv_never_sets_a_network_flag_by_default` and a leakage regression proving the variable never appears as a container `--env=`). The benchmark harness (`tests/fixtures/bench_target/bench_sandboxed_app.py`, `SandboxedBenchApp`) creates a uniquely-named, `--internal` (no route to the host or the internet) Podman network per test run, runs `bench_app.py` in its own throwaway container on it (no published host port), and tears both down after — verified end-to-end (real `tool_resolver.run_tool()` → `job_runtime` → `sandbox_runner` → real podman → real container → real differing SQLi evidence) before Task 12 began. All 6 real-tool tests now pass through the ACTUAL, unmodified MCP tool functions: SQLi (sqlmap-mcp), XSS (dalfox-mcp), IDOR (idor-mcp, pure-stdlib, no sandboxing needed), exposed backup file (ffuf-mcp), misconfiguration (nuclei-mcp, using the recovered `bench_misconfig_template` fixture that copies the template into the pinned, sandbox-mounted template root per `_resolve_templates_arg()`'s containment check), and open redirect (curl via `tool_resolver`) — each confirming its planted bug in vulnerable mode and correctly finding nothing in patched mode. **Two real, previously-undetected production bugs found and fixed via this real-tool testing (not just benchmark artifacts):** (1) `sqlmap-mcp test_injection()` unconditionally appended `--forms`, which this sqlmap version treats as a FORMS-ONLY scan when combined with `-u` and no `--crawl` — on any page without an HTML `<form>` (the common case for API/query-string endpoints), sqlmap aborted without ever testing the URL's own parameter, a silent false negative for every real hunt hitting a formless page; a first fix (bare removal of `--forms`) was itself caught in adversarial review as silently losing the documented form-auto-detection capability, then refined to add `--crawl=1` alongside `--forms` instead — confirmed by reproduction (with `--flush-session`, ruling out cached-result false positives) that this detects the injection without the abort AND preserves form-testing (regression: `test_sqlmap_mcp.py::test_injection_pairs_forms_with_crawl_so_it_never_aborts_on_a_formless_page`). (2) `dalfox-mcp scan_url()`/`scan_parameter()` passed `--format json`, which dalfox emits as a pretty-printed JSON ARRAY, not one-object-per-line — `_format_findings()`'s line-by-line parser therefore never parsed a single finding, always reporting "No XSS found" even when dalfox found and verified real XSS; fixed by switching to `--format jsonl` (regression: `test_dalfox_mcp.py::test_scan_url_and_scan_parameter_request_jsonl_not_pretty_json`). One test-harness-only gap also fixed (not a production bug): `_load_mcp_server()`'s dynamic-import helper didn't replicate CPython's automatic script-directory `sys.path[0]` insertion, breaking `idor-mcp/server.py`'s same-directory `idor_sweep.py` import — fixed in the test helper only. **Independent adversarial review (2026-09-23, security-review skill + 8-angle code-review skill, both run as fresh subagents):** security pass found zero exploitable vulnerabilities (confirmed `HUNTMCP_BENCH_NETWORK` has no code path from agent/target input, all argv construction stays list-based/non-shell). The broader correctness/quality pass found and fixed: (a) `HUNTMCP_BENCH_NETWORK`'s value was spliced into `--network=<value>` with no validation — a stray `"host"` value would have granted full host networking to every real Tier-2 call in that process; fixed with `sandbox_runner._validate_bench_network()` / a new `UnsafeBenchNetwork`, allowlisting only `SandboxedBenchApp`'s own generated-name prefix; (b) a unit test bound `0.0.0.0` directly in the host pytest process (contradicting bench_app's own loopback-only guarantee) — changed to `127.0.0.2` (still non-routable, proves the same code path); (c) the container-readiness poll spun up a brand-new throwaway container every ~0.5s and could leak an uncaught `TimeoutExpired` — switched to `podman exec` against the already-running container with the timeout handled; (d) `bench_sandboxed_app.py`'s cleanup now reuses `sandbox_runner.remove_container()` instead of a hand-rolled duplicate; (e) 14 near-identical `monkeypatch.setenv` pairs across real-tool tests consolidated into one `bench_env` fixture (mirroring `test_cem_benchmark.py`'s own `cem_env` pattern). Full regression after all fixes (`.venv/bin/python -m pytest tests/ -q`): **1688 passed, 5 pre-existing xfailed, 0 failures** — no regression anywhere, including S5/S6/S-GATE (`test_sandbox_runner.py`, `test_job_runtime.py`, `test_s_gate_adversarial.py`) and CEM coexistence. Ground-truth blindness/integrity, tool-agnostic scoring, and `novel_findings` separation all unchanged from Tasks 1–11. Nothing pushed; diff reviewed prior to commit per instruction. | S1 | §4, §5(P2), §11 |
| **P2-TEL** | [x] | Passive telemetry: HTTP/tool/wall-clock/token yield linked to findings (offline; 0 hot-path cost). `mcp-servers/telemetry.py` -- three pure, read-only functions, zero new hooks into either real subprocess chokepoint (`tool_resolver.run_tool()`/`job_runtime.start_job()`), enforced by a regression test (`test_telemetry_not_wired_into_hot_path_chokepoints`) grepping both files for the literal string `telemetry` rather than left as a documentation-only claim. `audit_log_totals()` aggregates already-written `audit.jsonl` entries (tool_calls count, wall_clock_ms sum, per-tool breakdown), resolving the same active-engagement-aware path `audit_log.log_call()` itself uses via `audit_log._resolve_path()` directly rather than re-deriving it -- `audit_log.py` is an S6 hook-tamper-protected file, so adding a *public* wrapper there mid-task triggered a live BLOCKED-by-hook-tamper-resistance refusal requiring a human-run `scripts/confirm-hook-edit.sh`; reusing its existing private resolver instead avoided that for a same-project, read-only reuse with zero behavior change to `audit_log.py` itself (confirms S6 is still live, not a regression to work around). `to_bench_cost()` adapts that aggregate into the exact `cost: dict[str, float]` shape `tests/fixtures/bench_target/bench_evaluator.py`'s `evaluate(cost=...)` already declared open for this task (P2-BENCH design spec: "Token/$ cost later (P2-TEL)") -- `requests` is honestly `tool_calls` under a documented, not-overclaimed limit (most Tier-2 tools don't expose a real per-HTTP-request count back to the caller yet). `finding_telemetry(finding_id)` links telemetry to a specific finding via two already-existing FKs rather than inventing a new store: `case_store.experiments.finding_id` (new `list_experiments()` getter) for tool/cost counts, and `case_store.cem_trials.finding_id` (new `count_cem_trials()` getter, a lightweight `COUNT(*)` sibling of `cem_load_state()`) for a REAL per-HTTP-request count -- `cem_trials` is confirmed exact by PHASE1-EXECUTION-PLAN.md's own M1 measurement ("trials_persisted == http_delta"). `http_requests` is `None` (not `0`) when a finding was never run through CEM, distinguishing "not measured" from "measured, zero requests" so a sqlmap/dalfox/nuclei-confirmed finding that never went through CEM doesn't silently read as zero-cost. `token_yield` is always caller-supplied, never measured by this module -- `budget_guard.py`'s own docstring already established why (no MCP server has visibility into the orchestrating agent's own token spend). **Independent adversarial review** (security-review skill + 8-angle code-review skill, both fresh subagents, same methodology as every prior task in this file): security pass found zero exploitable findings (all SQL parameterized, no attacker-reachable path to `finding_id`/file paths in this diff yet, `audit_log_totals()` only ever surfaces aggregate counts, never raw redacted content). Code-review pass found 4 findings, 2 CONFIRMED + 2 PLAUSIBLE, 3 fixed via TDD: (1) CONFIRMED efficiency -- `finding_telemetry()` was calling `cem_load_state()` (4 queries + JSON-decoding every condition/trial/verdict row) just to count trials, fixed via the new lightweight `count_cem_trials()`; (2) CONFIRMED reuse -- `telemetry.py` was re-deriving `audit_log.py`'s own path-resolution logic instead of calling it, fixed by calling `audit_log._resolve_path()` directly (the S6 block on the alternative public-wrapper approach is the finding above); (3) PLAUSIBLE -- `http_requests` conflated "none" with "not measured", fixed (above); (4) PLAUSIBLE -- `to_bench_cost()`'s `requests` field name invites misreading as a literal per-request count, left as-is (`no_change_needed`) since it intentionally matches the exact key name P2-BENCH's own design doc named for P2-TEL to fill, not a naming accident. Tests: `tests/test_telemetry.py` (9) + `tests/test_case_store.py` (+5: `list_experiments`/`count_cem_trials` coverage, including the never-defined/defined-zero/real-count three-way split) -- 14 new focused tests, all green; S1–S6/P2-BENCH tests untouched. Full regression: 1698 passed, 1 skipped, 5 xfailed, 0 failures attributable to this change -- 3 load-dependent flakes observed across two full-suite runs (`test_sandbox_runner.py`'s network-dependent container test, `test_nuclei_mcp.py`'s live-binary-timeout test, `test_watch_mcp.py`'s job-race timing test), each independently reproduced as passing in isolation both with and without this diff (`git stash -u`), matching this file's own already-documented precedent for exactly these three tests (S-GATE/P2-BENCH rows, above) -- not a regression. `ruff check`/`py_compile` clean on both changed files. **ROUND 2 (human-requested "review and audit" of the full uncommitted diff, same round-3-labeled pass documented in the P2-E1 row below -- `security-review`+`code-review` skills, 8 finder angles + 1 security pass):** found and fixed the sibling of `postmortem.py`'s TOCTOU bug -- `finding_telemetry(db_path=None)` called `case_store.get_finding()`/`list_experiments()`/`count_cem_trials()` each with the ORIGINAL (still-`None`) `db_path`, letting each of the three independently re-resolve the active engagement inside its own `_get_conn()` call; a mid-call engagement switch could silently blend cost/CEM-trial data from two different engagements' `case.db` files sharing the same numeric `finding_id`. Fixed by resolving `db_path` once via `case_store.resolve_db_path()` at the top of `finding_telemetry()` and passing the resolved value to all three calls. Test: `tests/test_telemetry.py` (+1, `test_finding_telemetry_resolves_db_path_once_not_per_call`, confirmed RED by capturing each call's actual received argument). Full regression after both this and the `postmortem.py`/`watch-mcp` round-3 fixes together: **1726 passed, 1 skipped, 5 xfailed, 0 failed.** Nothing committed or pushed pending human review. | P2-BENCH | §5(P2), §15 |
| **C1a** | [ ] | Evidence **provenance binding** — capture seam feeding `add_evidence`: **wire-level** provenance for structured sources (curl/`tool_resolver`, `browser-mcp`, `oob-mcp`, CEM `fetch_fn`, VAL-AUTHZ `http_probe`); **invocation-level** provenance for scanner-narrated output. *Why:* discovery-path evidence is model-narrated today. | — | §8, §9 |
| **P2-E1** | [ ] | **Hunt postmortem / self-evaluation** — analysis-only, read-only, no tools, no auto-retry, no self-modification, no policy mutation; evidence-cited (cites `audit_log`/`case_store`); planted-fixture precision/recall; independent cross-check vs raw stores; redaction verified. | P2-TEL, `case_store`, `dedupe_check` | §5(P2 spec) |
| **P2-INJ (UU-7)** | [ ] | Tool-output-injection sanitization / quarantine. *Why:* live injection surface. | — | §4, §5(P2) |
| **P2-NK** | [ ] | Negative-knowledge + capability-utilization signal. | P2-TEL | §5(P2) |
| **P2-COV** | [ ] | **Minimal coverage instrument (H3 core)** — produce the coverage signal used in P2/P3. *Why:* metric must exist before it is a decision signal. | P2-BENCH | §4, §5(P2), §12 |
| **P2-SC** | [ ] | Supply-chain: pin tool versions + SBOM; Python SAST/dep-audit in CI; CI `permissions:`; `data/watch.db` untrack + per-engagement path. | — | §4, §6(P2 hardening) |
| **C1b** | [ ] GATED | Full research-run manifest (tool/model/policy versions + target-snapshot hash), instrumented for the acceptance A/B. **Do not promote without evidence.** | C1a, P2-BENCH | §8, §9, §11 |

---

## 5. Phase 3 — Discovery Amplification, Authorization Differential + CEM-in-Hunt

*Prereq: Phase-2 substrate; **Phase-S Tier-2 (S-GATE) complete before breadth begins.***

| ID | St | Task · Why | Deps | v3 § |
|---|---|---|---|---|
| **P3-DEP** | [!] | **Human decision:** approve `schemathesis` (and a spec parser only if hand-rolled proves insufficient) per dependency-budget. *Why:* new dependency = stop-condition. | — | §5(P3), §14.6 |
| **P3-SCHEMA** | [ ] GATED | schemathesis→CEM stateful/API property falsifiers. *Complementary to VAL-AUTHZ (property-fuzz vs authz-differential).* | P3-DEP, S-GATE, P2-BENCH | §4, §5(P3) |
| **P3-ADAPTER** | [ ] | property→hypothesis→CEM adapter (each falsifier hit becomes a hypothesis fed to CEM). | P3-SCHEMA | §5(P3) |
| **P3-CEMHUNT** | [ ] | CEM auto-run after independent validation **preserving the existing CEM entry precondition** (`CONFIRMED`/determinism gate). *Not a CEM change.* | CEM (frozen), case_store | §5(P3), §7 |
| **P3-DIFF** | [ ] | Structural differentiation evidence vs disclosed reports (dedupe aid). | `disclosed_reports` | §4, §5(P3) |
| **P3-DISCSPEC** | [ ] | **DISC-SPEC** — parse OpenAPI/GraphQL into a stateful endpoint/parameter model (hand-rolled first; treat spec as untrusted) feeding scan targeting **and VAL-AUTHZ workflow capture**. | — | §4, §5(P3) |
| **P3-VALAUTHZ** | [ ] GATED | **VAL-AUTHZ (min)** — stateful/multi-step **authorization differential**: (a) strengthen sweep oracle to **owner-fetch-and-compare**; (b) **two-step replay** (capture workflow as identity A; replay step N under identity B holding earlier steps); (c) role/identity matrix (unauth/low-priv/admin/tenant-B). Emit findings to `case_store.create_finding` with CEM conditions (identity, step-order, carried id). **Idempotent/read-GET default; state-changing steps require human gate; `scope_guard` every request.** *Must NOT be `[x]` merely because code exists — the benchmark must show multi-identity/stateful differential behavior at zero new FP.* | S-GATE, P2-BENCH, benefits from P3-DISCSPEC; extends `idor-mcp` | §4, §5-VA, §11, §12 |
| **P3-FANOUT** | [ ] | **Bounded, dedupe-aware** parallel fan-out (concurrency for discovery only). *Guardrail:* bounded; **unrestricted parallelism remains rejected (§13).** | P2-BENCH | §13 (bounded only) |
| **P3-H4** | [ ] | Scan-identification headers (target-only, correct destination boundary). | — | §4, §5(P3) |
| **P3-ASSETGRAPH** | [!]/REJECTED | "Asset Graph" as a graph-DB substrate is **NOT a committed v3 capability** — `chainer-mcp` is the DAG; graph DB is rejected (§13). Listed to avoid silent omission; no active task unless a future human decision revisits it. | — | §13, §10-A |

---

## 6. Phase 4 — Application Reasoning + Coverage

*Prereq: Phase-2 telemetry + coverage instrument. M0 failure blocks ONLY M1 — not the independent P4 items.*

| ID | St | Task · Why | Deps | v3 § |
|---|---|---|---|---|
| **P4-M0** | [ ] GATED | Invariant-model **prototype** (shallow) → GO/NO-GO. *Why:* invariant inference is the least-tractable bet. | P2-TEL, P2-BENCH | §4, §5(P4) |
| **P4-M1** | [!] | Full invariant model — **blocked on P4-M0 GO only**. Invariants remain **CEM-provable hypotheses**; invariant **false positives are a quality risk**, not findings. | P4-M0 GO | §5(P4) |
| **P4-RESURRECT** | [ ] | Hypothesis resurrection — additive `case_store` query (no new store). **Independent of M0.** | case_store | §4, §5(P4) |
| **P4-INFOGAIN** | [ ] GATED | Info-gain experiment **ordering** — **orders, never hard-blocks a novel test.** **Independent of M0.** | P2-TEL | §4, §5(P4), §11-A |
| **P4-SIG** | [ ] | Causal signatures from CEM minimal-sets (feeds P5 transfer/variants). | CEM (frozen) | §5(P4/P5) |
| **P4-ESCALATE** | [ ] | Human escalation as an **information-value** mechanism (not approval-fatigue). | P2-TEL | §5(P4) |
| **P4-BM** | [ ] | **Blind, independent** long-horizon benchmark (sustained investigation, multi-identity, business-logic, target-change, hypothesis persistence/resurrection, recovery, quality of a correct no-finding). | P2-BENCH | §5(P4), §11 |
| **P4-COVMATRIX** | [ ] | Full coverage matrix endpoint×class×role (H3) **with anti-gaming clause**. | P2-COV | §4, §5(P4) |
| **P4-PM+** | [ ] | Postmortem **extensions** (e.g. `missed_chain_opportunities`) — *extends* P2-E1, analysis-only. | P2-E1 | §5(P4) |
| **P4-VALCOND** | [ ] DEFERRED | Autonomous CEM condition extraction — human-supplied default retained; parity gate (no new false necessity). | CEM, P2-BENCH | §4 (VAL-COND) |

---

## 7. Phase 5 — Adaptive Allocation + Continuous / Delta *(GATED research program — not guaranteed builds)*

| ID | St | Task · Why | Deps | v3 § |
|---|---|---|---|---|
| **P5-A0** | [!] | **STOP/GO** decision to authorize the allocation program. | P4 measurement | §5(P5), §11 |
| **P5-A1** | [ ] GATED | **A/B/C/D experiment** (A frontier-heavy · B fixed-mixed · C adaptive · D CEM-assisted), held-out/blind; **C-vs-D kept separate = the entire attribution of CEM's contribution**. | P5-A0 GO, P4-SIG | §5(P5), §11 |
| **P5-ALLOC** | [!] | Adaptive allocation policy — **only after P5-A0 GO and passing the floor+invariants**. Never suppress a novel test; cheap-tier premature-closure guard. | P5-A0 GO, P5-A1 | §5(P5), §11-A |
| **P5-T1** | [ ] GATED | CEM signature-transfer experiment (same/similar/cross-target). Gate: **false-reuse→missed-test == 0.** | P4-SIG, P5-A0 | §5(P5), §11 |
| **P5-AMORT** | [!] | CEM amortization / signature reuse — **only after P5-T1 proves safe transfer**. | P5-T1 pass | §5(P5) |
| **C3** | [ ] GATED | Continuous monitoring + **delta re-validation** (watch↔case link → affected hypotheses → CEM re-run) + patch-bypass regression. Gate: re-hunt precision > cold start. | C1a (should-use), watch↔case link | §4, §5(P5), §9 |
| **P5-VARIANT** | [ ] GATED | Active counterfactual variant discovery (DISC-VARIANT) — rides CEM; zero double-report. | CEM, P4-SIG | §4, §5(P5) |
| **P5-XTARGET** | [ ] DEFERRED | Cross-target technique induction — no cross-target contamination; gated by P5-T1. | P5-T1 | §4, §5(P5) |
| **P5-ANTIANCHOR** | [ ] | Anti-anchoring — realized via C-vs-D attribution + blind ground truth (not a separate engine). | P5-A1 | §5(P5), §11-A |
| **P5-ROUTER** | [ ] DEFERRED | Skill Router / capability-exposure layer — DEFERRED to V4 (trigger conditions). | trigger | §4 (V4) |
| **P5-TOOLKIT** | [~] | Human-gated toolkit growth — `tool_gaps.py` capture exists (bounded first step); **growth requires human approval per addition**. | tool_gaps, recurrence signal | §4, §5(P5) |

---

## 8. Human-Decision Blockers `[!]`

| ID | Decision required | Blocks |
|---|---|---|
| P3-DEP | approve `schemathesis` / spec-parser dependency | P3-SCHEMA (breadth) |
| (S5 dep) | ~~approve rootless runtime dependency~~ **RESOLVED 2026-09-15: Docker/Podman rootless containers** (over bubblewrap/nsjail; microVM excluded per §6 non-goals) | S5/S6 Tier-2 — unblocked |
| P4-M0 | invariant-model GO/NO-GO | P4-M1 only |
| P5-A0 | allocation STOP/GO | P5-A1/ALLOC |
| P5-T1 | transfer-safety promotion | P5-AMORT/P5-XTARGET |
| C1b | manifest promotion (A/B outcome) | C1b build |
| ROADMAP-INT | authorize folding v3 into protected docs (`ROADMAP.md`/proposal/exec-plan) | canonicalization |
| V4-TRIGGER | capability-exposure layer (3rd runtime / drift) | P5-ROUTER |

*Not converted into "already decided" implementation tasks.*

---

## 9. Dependency Graph

```
[x] CEM Phase 1 (FROZEN)  ── consumed everywhere, modified nowhere
        │
S Tier-1 (S1 S2 S3 S4)                 ← HARD GATE before any autonomous breadth
        ↓
Phase 2 (P2-BENCH · P2-TEL · C1a · P2-E1 · P2-INJ · P2-NK · P2-COV · P2-SC ; C1b gated)
        │
S Tier-2 (S5 S6 → S-GATE)              ← GATE before Phase-3 breadth
        ↓
Phase 3 (P3-DEP[!] → P3-SCHEMA · P3-ADAPTER · P3-CEMHUNT · P3-DIFF · P3-DISCSPEC → P3-VALAUTHZ · P3-FANOUT · P3-H4)
        ↓
Phase 4 (P4-M0 →(GO) P4-M1 ;  P4-RESURRECT · P4-INFOGAIN · P4-SIG · P4-ESCALATE · P4-BM · P4-COVMATRIX · P4-PM+  independent of M0)
        ↓
Phase 5 (P5-A0[!] → P5-A1 → P5-ALLOC ;  P5-T1 → P5-AMORT/P5-XTARGET ;  C3 ; P5-VARIANT ; P5-TOOLKIT)
```

Independent-not-serialized: P2 items run in parallel; P4-RESURRECT/INFOGAIN/BM/COVMATRIX do not wait on P4-M0; P3-H4 is
off the critical path; C3 needs only C1a + the watch↔case link (not the whole of P4).

---

## 10. Acceptance-Evidence Matrix

*Every non-trivial task's required evidence. "n/a" = category not applicable, with reason.*

| ID | Implementation | Tests | Runtime verification | Adversarial / security | Acceptance gate |
|---|---|---|---|---|---|
| S1 | fail-closed target-touch path | unit: block on error | live: gated call blocked | inject malformed/hostile hook input | out-of-scope call blocked; benign session not broken |
| S2 | CI job | CI smoke test | CI run | out-of-scope fixture call | CI fails if not blocked |
| S3 | per-key secret injection | unit | live: tool sees only its key | compromised-agent `cat .env` test | sandboxed exec cannot read `.env` |
| S4 | confirm tier | unit | live prompt | attempt os-shell w/o confirm | RCE requires explicit confirm |
| S5/S6/S-GATE | rootless boundary + tamper-resist | unit | lifecycle run | **adversarial regression corpus** | containment **AND** utility; lifecycle-stable |
| P2-BENCH | multi-class range | harness self-test | run baseline | ground-truth blind/protected | frontier arm + labels reproducible |
| P2-TEL | telemetry emit | unit | offline capture | n/a (offline; no hot-path) | cost/yield/coverage emitted, 0 hot-path cost |
| C1a | capture seam | unit | live: evidence↔exchange bound | tamper/forge attempt | discovery evidence carries strongest-available provenance |
| P2-E1 | offline analyzer | unit | **planted-fixture recall** | prove read-only (no writes/tools) | recall bar; independent cross-check clean; redaction verified |
| P2-INJ | sanitizer | unit | injection regression | hostile tool-output corpus | injection regression passes |
| P2-COV | coverage signal | unit | run on benchmark | anti-gaming probe | signal produced + benchmarked |
| P2-SC | pin+SBOM+SAST | CI | CI run | dependency-audit | pinned build; SAST in CI |
| C1b | manifest | unit | A/B instrumentation | n/a | **triager-acceptance A/B** (promote only on uplift) |
| P3-SCHEMA | schemathesis→CEM | unit | WFC/WFD run | n/a | breadth up; **quality floor held**; FCC==0 |
| P3-CEMHUNT | wiring | unit | in-hunt run | verify entry precondition intact | CEM runs in-hunt; FCC==0 |
| P3-VALAUTHZ | oracle+replay+matrix | unit | run on benchmark | state-changing → human gate; scope on every req | **recovers multi-step authz the sweep misses; ZERO new FP; verdicts match ground truth**; rollback flag→sweep |
| P3-DISCSPEC | spec parser | unit | endpoints recovered | spec treated as untrusted | measurable surface gain feeding authz/scan |
| P4-M0 | prototype | unit | blind benchmark | n/a | GO/NO-GO recorded |
| P4-M1 | invariant model | unit | blind benchmark | invariant FP is a quality risk | quality floor held; invariants stay CEM-provable |
| P4-RESURRECT | query | unit | run | n/a | machine-checkable resurrection predicate |
| P4-INFOGAIN | ordering | unit | run | never hard-blocks a novel test | measurable uplift; novel tests never suppressed |
| P4-BM | benchmark | harness | run | blind/independent | protected ground truth; anti-gaming |
| P4-COVMATRIX | matrix | unit | run | anti-gaming clause | non-gameable coverage |
| P5-A1 | A/B/C/D | harness | held-out run | blind | C-vs-D attribution under floor+invariants |
| P5-T1 | transfer exp | harness | 3 conditions | stale-signature-never-authoritative-skip | **false-reuse→missed-test == 0** |
| C3 | watch↔case + re-run | unit | delta-injection run | no cross-target leakage | re-hunt precision > cold start |
| P5-TOOLKIT | gated growth | unit | run | human approval per addition | no self-modification; human-gated |

---

## 11. Safety / Quality Floors (apply to all applicable tasks; never weakened)

**Absolute security invariants (zero-tolerance):** CEM **FCC == 0**; **false-reuse→missed-test == 0**;
allocation/dedupe/negative-knowledge **never hard-block a not-yet-confirmed novel test**; **cheap-tier
premature-closure guard**; non-idempotent/state-changing actions require appropriate approval (human gate);
**no self-modifying execution**; **no uncontrolled cross-target leakage**; postmortem is analysis-only;
human-review-before-submit preserved.
**Quality floor (hard-fail on high/critical):** high/critical recall ≥ baseline − ε; FP ≤ baseline; coverage ≥
baseline (P3, P5).
**Benchmark integrity:** blind, protected, evaluator-only ground truth; no auto-derived oracle; anti-gaming
(`.claude/rules/benchmarks.md`). Any change to protected benchmark methodology = human approval.

---

## 12. Overall Progress Summary

| Phase | [x] | [~] | [ ] | [!] | Notes |
|---|---|---|---|---|---|
| CEM Phase 1 | 1 (frozen) | — | — | — | do not rebuild |
| Baseline substrate | 8 | 6 | — | — | substrate ≠ roadmap capability |
| Phase S | 7 (S1–S6, S-GATE) | — | — | — | Tier-1 + Tier-2 + exit gate all complete; Phase-3 breadth unblocked pending human PR review/merge |
| Phase 2 | — | 1 (P2-BENCH, substrate only — Tasks 1-11/17) | 7 | — | +C1b GATED |
| Phase 3 | — | — | 8 | 1 (P3-DEP) | VAL-AUTHZ GATED; Asset-Graph rejected |
| Phase 4 | — | — | 8 | 1 (P4-M1 on M0) | +VAL-COND deferred |
| Phase 5 | — | 1 (toolkit) | 6 | 3 (A0/ALLOC/AMORT) | gated research program |
| Human blockers | — | — | — | 8 | §8 |

**Phase-S Tier-1 (S1–S4) is `[x]`, CI-verified. S5 (rootless per-run execution boundary, Tier-2) is also `[x]`,
verified with real end-to-end sandboxed calls through unmodified MCP server code (§3). S6 (hook tamper-resistance,
Tier-2) is also `[x]`: Vulns 1–4 (found via two rounds of post-landing adversarial security review) implemented +
tested + live-dispatch-verified on both harnesses; Vuln 5 (`git checkout <ref>`/`git reset --hard`) explicitly
decided by the human as "document and defer" — recorded in the code's own docstring + 2 regression tests, not
silently skipped — merged to `main` via PR #111 (`ca8568f`).**
**S-GATE is now also `[x]`:** the Tier-2 exit gate's adversarial regression suite (`tests/test_s_gate_adversarial.py`,
15 tests) plus two rounds of independent adversarial security review (identical vuln-ID + parallel-verification-
subagent methodology as S5/S6's own rounds) found and fixed, via TDD, 3 real bugs — 2 pre-existing HIGH-severity
bypasses in production `scope_gate_hook.py` (a scope-check bypass and an S6 hook-tamper-resistance bypass, both the
same "wrapper word defeats first-word matching" class already fixed for S4's sqlmap check but never ported to these
two other checks) and 1 regression introduced by round 1's own fix (a `timeout --signal=KILL ...`-shaped flag
swallowing the real command) — plus documented one accepted, fail-closed-direction trade-off. Every bypass from
both rounds was independently re-verified live by hand against the real hook binary. Full regression clean: 1610
passed, 0 failed, 5 xfailed. See §3's S-GATE row for the complete account. Verified: [PR #112]
(https://github.com/ankitsingh015/HuntMCP/pull/112), merged `b002369` — no roadmap capability past Phase-S
(Tier-1 + Tier-2 + S-GATE), no gated item, no human decision is treated as resolved beyond that. **Phase-3
breadth work (VAL-AUTHZ's live requests, schemathesis) may now begin, subject to its own prerequisites (P2
substrate, `P3-DEP` human approval of `schemathesis`, benchmark ground truth) still being unmet** (§3, §9 —
S5+S6 individually did not satisfy S-GATE; the combined adversarial regression now does).

---

## 13. Final Check (per task §17)

- ✅ CEM Phase-1 marked `[x] FROZEN` — **not** decomposed or duplicated.
- ✅ No roadmap capability silently omitted — every v3 §4 matrix item + §10-A older-stream item is represented
  (VAL-AUTHZ present as P3-VALAUTHZ GATED; DISC-SPEC as P3-DISCSPEC; VAL-COND deferred; DISC-VARIANT as P5-VARIANT;
  ARCH-STATE = the P2-TEL/C1a provenance area + a deferred recon/scan-wiring note; LEARN-KG/graph rejected;
  Asset-Graph rejected).
- ✅ No partial/unverified implementation marked `[x]` — S1–S4 are `[x]` only with CI-verified evidence cited per-task
  (§3); evidence-integrity (content-addressed evidence, provenance binding) remains `[~]`/`[ ]` since C1a hasn't
  started.
- ✅ No gated research marked `[x]` (P3-SCHEMA, P3-VALAUTHZ, C1b, P4-M0, P5-A1, P5-T1, C3, P5-VARIANT all GATED/`[ ]`).
- ✅ No human decision treated as resolved (P3-DEP, P4-M0, P5-A0, P5-T1, C1b, ROADMAP-INT, V4 all `[!]`).
- ✅ **VAL-AUTHZ present** (P3-VALAUTHZ, GATED, multi-identity/stateful benchmark).
- ✅ **P2-E1 present** (full analysis-only guardrails).
- ✅ **Security Tier-1 → Tier-2 → Phase-3-breadth ordering present** (§3, §9); Tier-2 not optional.
- ✅ Quality floors + absolute invariants present (§11).
- ✅ Benchmark gates present (§10, §11).
- ✅ P5 remains a gated research program (§7).

*Output file: `IMPLEMENTATION-TASK-TRACKER.md`. This document itself has evolved alongside real implementation work
since its initial planning-only draft — Phase-S Tier-1 (S1–S4) is now `[x]`, CI-verified, with PR/commit evidence
cited per-task in §3. Nothing beyond Tier-1 has been implemented; Phase 2 onward, all gated items, and all human
decision blockers remain exactly as originally scoped.*
